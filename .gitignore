# Node.js 의존성 파일 (필수)
node_modules/

# 빌드 결과물 디렉토리 (필수)
dist/
build/

# SVG 파일 무시 (필요한 경우 주석 처리)
# *.svg

# VS Code 관련 파일 및 폴더 무시 (필수)
.vscode/

# JSON 파일 무시 (모든 JSON 파일을 제외하는 경우, 필요하지 않다면 주석 처리)
# *.json

# yarn.lock 파일 무시 (필요한 경우 주석 처리, 일반적으로 포함해야 함)
# yarn.lock

# Vite 설정 파일 무시 (필요한 경우 주석 처리, 일반적으로 포함해야 함)
# vite.config.js

# macOS에서 자동 생성되는 파일 무시 (필수)
.DS_Store
